# household-os frontend

React frontend for household-os, built on the Claude Design
component library (kept current via design-sync — see ADR-0002).

> **Not scaffolded yet.** The app shell (Vite/build tooling, package manager,
> Claude Design integration) is the next step.
